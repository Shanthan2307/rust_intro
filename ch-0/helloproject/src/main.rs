fn main() {
    println!("Hello, shanthan   from cargo!");
    println!("Whenever using an individual rust file we have to compile it first and then it will execute");
    println!("But whenever we're using a rust project just use cargo run it'll compile and run it at the same time");
    println!("New projects can be created using cargo new <project_name>");
    println!("To build the project use cargo build(This only compiles but it does not run it can be used for testing compilation errors)");
    println!("To run the project use cargo run(This will compile and run the project at the same time)");
}
